# Medicare IoT — ML Service Requirements
# CPU-only PyTorch (much smaller than GPU build, fits HF Spaces free tier)

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# PyTorch CPU-only (saves ~800MB vs full build)
torch>=2.2.0 --index-url https://download.pytorch.org/whl/cpu

# Anomaly detection
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.2.0
