# Lightweight Production requirements for Vercel Serverless Functions
# ML inference is handled by the remote HuggingFace Spaces microservice (ml_service/)
# PyTorch / scikit-learn intentionally excluded — they exceed Vercel's 250MB limit
flask>=3.0
flask-sqlalchemy>=3.1
sqlalchemy>=2.0
requests>=2.31
supabase>=2.3
python-dotenv>=1.0
psycopg2-binary>=2.9
gunicorn>=21.2
